# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Environment
.env

# Runtime SQLite dir at the repo root (mounted into the container as
# /app/data). `server/data/` holds tracked resources — prompt .txt
# files and llm_prices.json — and stays versioned, hence the leading
# slash to anchor this rule to the repo root only.
/data/

# Firmware binary + version.txt pushed by CI for OTA
firmware/

# IDE
.vscode/
.idea/

# Claude Code session worktrees
.claude/
